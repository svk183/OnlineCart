"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const schematics_1 = require("@angular-devkit/schematics");
const workspace_1 = require("@nrwl/workspace");
const testing_1 = require("@angular-devkit/schematics/testing");
const workspace_2 = require("@nrwl/workspace");
const path_1 = require("path");
describe('Update 8.5.0', () => {
    let tree;
    let schematicRunner;
    beforeEach(() => {
        tree = new testing_1.UnitTestTree(schematics_1.Tree.empty());
        schematicRunner = new testing_1.SchematicTestRunner('@nrwl/workspace', path_1.join(__dirname, '../../../migrations.json'));
    });
    describe('Update Angular CLI', () => {
        it('should update @angular/cli', () => __awaiter(this, void 0, void 0, function* () {
            tree.create('package.json', workspace_2.serializeJson({
                devDependencies: {
                    '@angular/cli': '8.0.0'
                }
            }));
            const result = yield schematicRunner
                .runSchematicAsync('upgrade-cli-8-3', {}, tree)
                .toPromise();
            expect(workspace_1.readJsonInTree(result, 'package.json').devDependencies['@angular/cli']).toEqual('^8.3.3');
        }));
        it('should coerce a version with a caret into a valid version', () => __awaiter(this, void 0, void 0, function* () {
            tree.create('package.json', workspace_2.serializeJson({
                devDependencies: {
                    '@angular/cli': '^8.0.0'
                }
            }));
            const result = yield schematicRunner
                .runSchematicAsync('upgrade-cli-8-3', {}, tree)
                .toPromise();
            expect(workspace_1.readJsonInTree(result, 'package.json').devDependencies['@angular/cli']).toEqual('^8.3.3');
        }));
        it('should coerce a version with a tilde into a valid version', () => __awaiter(this, void 0, void 0, function* () {
            tree.create('package.json', workspace_2.serializeJson({
                devDependencies: {
                    '@angular/cli': '~8.0.0'
                }
            }));
            const result = yield schematicRunner
                .runSchematicAsync('upgrade-cli-8-3', {}, tree)
                .toPromise();
            expect(workspace_1.readJsonInTree(result, 'package.json').devDependencies['@angular/cli']).toEqual('^8.3.3');
        }));
        it('should fail if the version cannot be validated', () => __awaiter(this, void 0, void 0, function* () {
            let error = null;
            tree.create('package.json', workspace_2.serializeJson({
                devDependencies: {
                    '@angular/cli': '>=8.0.0'
                }
            }));
            try {
                yield schematicRunner
                    .runSchematicAsync('upgrade-cli-8-3', {}, tree)
                    .toPromise();
            }
            catch (e) {
                error = e;
            }
            expect(error).toStrictEqual(new Error(`The package.json lists a version of @angular/cli that Nx is unable to validate - (>=8.0.0)`));
        }));
    });
});
