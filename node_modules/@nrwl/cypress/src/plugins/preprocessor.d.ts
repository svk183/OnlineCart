import { TsconfigPathsPlugin } from 'tsconfig-paths-webpack-plugin';
export declare function preprocessTypescript(config: any): any;
export declare function getWebpackConfig(config: any): {
    resolve: {
        extensions: string[];
        plugins: TsconfigPathsPlugin[];
    };
    module: {
        rules: {
            test: RegExp;
            loader: string;
            exclude: RegExp[];
            options: {
                configFile: any;
                experimentalWatchApi: boolean;
            };
        }[];
    };
    plugins: any[];
    externals: any[];
};
