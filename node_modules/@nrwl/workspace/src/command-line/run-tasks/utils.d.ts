import * as yargs from 'yargs';
import { NxJson, ProjectNode } from '../shared';
export interface Arguments<T extends yargs.Arguments> {
    nxArgs: T;
    targetArgs: T;
    overrides: any;
}
export declare function splitArgs<T extends yargs.Arguments>(args: T, nxSpecific: (keyof T)[]): Arguments<T>;
export interface TaskArgs {
    projects: string[];
    target: string;
    all?: boolean;
}
export declare function projectHasTargetAndConfiguration(project: ProjectNode, target: string, configuration?: string): any;
export interface Environment {
    nxJson: NxJson;
    workspaceJson: any;
    workspace: any;
}
export declare function readEnvironment(target: string): Environment;
