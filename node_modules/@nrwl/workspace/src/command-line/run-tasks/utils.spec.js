"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("./utils");
describe('splitArgs', () => {
    it('should split nx specific arguments into nxArgs', () => {
        expect(utils_1.splitArgs({
            files: [''],
            notNxArg: true,
            _: ['--override'],
            $0: ''
        }, ['files']).nxArgs).toEqual({
            files: ['']
        });
    });
    it('should split non nx specific arguments into target args', () => {
        expect(utils_1.splitArgs({
            files: [''],
            notNxArg: true,
            _: ['--override'],
            $0: ''
        }, ['files']).targetArgs).toEqual({
            notNxArg: true
        });
    });
    it('should split delimited args into task overrides', () => {
        expect(utils_1.splitArgs({
            files: [''],
            notNxArg: true,
            _: ['', '--override'],
            $0: ''
        }, ['files']).overrides).toEqual({
            override: true
        });
    });
});
