import * as Lint from 'tslint';
import { IOptions } from 'tslint';
import * as ts from 'typescript';
import { ProjectNode } from '../command-line/shared';
import { Deps } from '../utils/runtime-lint-utils';
export declare class Rule extends Lint.Rules.AbstractRule {
    private readonly projectPath?;
    private readonly npmScope?;
    private readonly projectNodes?;
    private readonly deps?;
    constructor(options: IOptions, projectPath?: string, npmScope?: string, projectNodes?: ProjectNode[], deps?: Deps);
    apply(sourceFile: ts.SourceFile): Lint.RuleFailure[];
}
